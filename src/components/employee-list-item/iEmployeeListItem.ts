import { IconType } from 'react-icons/lib/esm/iconBase';
import internal from 'stream';

export interface EmployeeListItemComponent {
  name: string;
  salary: internal;
  function: string;
  bonus: boolean;
}
